fastapi>=0.104.1          # FastAPI 核心
uvicorn[standard]>=0.24.0 # ASGI 服务器（运行 FastAPI）
requests>=2.31.0          # 高德API/网络请求
openai>=1.3.7             # 通义千问API
python-multipart>=0.0.6   # （可选）文件上传支持
pillow>=10.1.0            # （可选）图片压缩/处理
psycopg2-binary>=2.9.9    # （可选）PostgreSQL 依赖（替代 SQLite 持久化）
